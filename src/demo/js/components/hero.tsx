import * as React from "react";

import * as A from "../../../lib";

const Hero = () => (
    <A.Hero title="Antwerp Core React Branding" subtitle="Overview of the styles" color="#ddbbbb">
    </A.Hero>
);

export default Hero;
