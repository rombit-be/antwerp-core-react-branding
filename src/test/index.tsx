import * as React from "react";
import * as ReactDOM from "react-dom";

const mountId = "mount";

// Render the main document...
ReactDOM.render(<div>Bonjour</div>, document.getElementById(mountId));
